<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:clipChildren="false"
    android:clipToPadding="false"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageButton
        android:layout_width="@dimen/sp_5xl"
        android:layout_height="@dimen/sp_5xl"
        android:layout_marginBottom="@dimen/sp_3xl"
        android:background="@drawable/shape_squircle_active"
        android:contentDescription="@string/content_room"
        android:elevation="@dimen/sp_3xl"
        android:src="@drawable/bx_bowl_hot"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>